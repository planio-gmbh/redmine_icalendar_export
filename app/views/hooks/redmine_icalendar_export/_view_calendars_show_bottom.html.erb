<% if User.current.allowed_to?(:view_calendar, @project, :global => true)%>
	<% other_formats_links do |f| %>
    <%= f.link_to 'ICS', :url => { :controller => 'i_calendar', :action => 'index', :status => 'all',  :assigned_to => '*', :project_id => @project, :key => User.current.rss_key, :format => 'atom' } %>
	<% end %>
<% end %>
